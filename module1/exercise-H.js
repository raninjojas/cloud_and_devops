// This function states the name and the origin country by taking two strings (name & country) and return a string //
function createFirstfunction(name, country) {
    const message =
      "My Name is " + name + " and I am from " + country;
    return message;
  }
  console.log(createFirstfunction("Ranin", "Palestine"));